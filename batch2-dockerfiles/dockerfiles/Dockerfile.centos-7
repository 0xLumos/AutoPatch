FROM centos:7

LABEL maintainer="test@example.com" \
      description="CentOS 7 with httpd"

RUN yum update -y && yum install -y \
    httpd \
    curl \
    wget \
    && yum clean all \
    && rm -rf /var/cache/yum/*

EXPOSE 80 443

WORKDIR /var/www/html

CMD ["httpd", "-D", "FOREGROUND"]
